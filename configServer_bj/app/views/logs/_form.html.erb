<%= form_for(@log) do |f| %>
  <% if @log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
      <% @log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :log_value %><br />
    <%= f.text_field :log_value %>
  </div>
  <div class="field">
    <%= f.label :dev_id %><br />
    <%= f.text_field :dev_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
